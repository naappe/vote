import type {ActivityItem} from '../lib/types';
const icons={call:'☎',visit:'⌂',vote:'✓',remark:'✎'};
export default function ActivityFeed({items}:{items:ActivityItem[]}){return <section className="panel"><div className="mb-5"><p className="muted">Latest work</p><h2 className="section-title">Activity</h2></div><div className="space-y-3">{items.map(item=><article key={item.id} className="flex gap-3 rounded-2xl bg-slate-50 p-3 transition hover:bg-calm-50"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-white text-calm-700 shadow-sm">{icons[item.type]}</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><b className="text-sm">{item.title}</b><time className="text-xs text-slate-400">{item.time}</time></div><p className="mt-1 text-xs leading-5 text-slate-500">{item.detail}</p></div></article>)}</div></section>}
