import type {DashboardStats} from '../lib/types';
export default function ReachProgressBar({stats}:{stats:DashboardStats}){const pct=stats.total?Math.round(stats.reached/stats.total*100):0;return <section className="panel"><div className="flex items-end justify-between"><div><p className="muted">Overall contact</p><h2 className="section-title">Resident reach</h2></div><strong className="text-3xl text-calm-700">{pct}%</strong></div><div className="mt-5 h-4 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-calm-500 transition-all duration-700" style={{width:`${pct}%`}}/></div><div className="mt-3 flex justify-between text-sm text-slate-500"><span>{stats.reached.toLocaleString()} reached</span><span>{stats.notReached.toLocaleString()} remaining</span></div></section>}
